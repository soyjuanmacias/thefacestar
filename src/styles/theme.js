
import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  fonts: {
    logo: `'copperplate', sans-serif`,
    // heading: `'Heading Font Name', sans-serif`,
    // body: `'Body Font Name', sans-serif`,
  },
})

export default theme